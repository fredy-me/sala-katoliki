# Project-specific R8 rules belong here when a release build identifies a
# dependency that requires one. Flutter's generated Android keep rules remain
# in effect alongside the optimized Android default rules.
